server:
  port: 8083
spring:
  application:
    name: studyParty-group
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://studyparty.pg.polardb.rds.aliyuncs.com:5432/studyparty?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&currentSchema=public
    username: root_yzt
    password: Yzt9345245374
  cloud:
    nacos:
      username: nacos
      password: nacos
      config:
        server-addr: nacos:8848
      discovery:
        enabled: true
        namespace: 086bcf84-a9bb-4265-aa7c-98fcad205276
        server-addr: nacos:8848
  data:
    redis:
      port: 6379
      timeout: 6000 # ??????????
      host: redis
  kafka:
    bootstrap-servers: kafka:9092 # Kafka服务器地址
    consumer:
      group-id: studyParty # 消费组ID
      auto-offset-reset: earliest # 当没有初始偏移量时该做何处理（earliest, latest）
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
mybatis-plus:
  type-aliases-package: com.studyParty.entity.group;
  mapper-locations: classpath:Mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
springdoc:
  api-docs:
    path: /v3/api-docs  # ??????????????
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.studyParty.group.controller  # ??????Controller?
  swagger-ui:
    path: /swagger-ui.html
    config-url: /v3/api-docs/swagger-config
dubbo:
  application:
    name: studyParty-group # 应用名称
  registry:
    address: nacos://nacos:8848?username=nacos&password=nacos&namespace=086bcf84-a9bb-4265-aa7c-98fcad205276 # 注册中心地址
  config-center:
    address: nacos://nacos:8848?username=nacos&password=nacos&namespace=086bcf84-a9bb-4265-aa7c-98fcad205276 # 配置中心地址
  protocol:
    name: dubbo
    port: 20880 # 服务暴露端口
  scan:
    base-packages: com.studyParty.dubboApi.service # 扫描服务接口所在包
# 服务提供方
dubbo.provider:
  timeout: 2000 # 服务提供方超时时间
# 服务消费方
dubbo.consumer:
  timeout: 2000 # 服务消费方超时时间
knife4j:
  enable: true  # ??Knife4j??UI
  setting:
    language: zh_cn
head: static/head/
saveHead: /root/studyParty/head/
# 自定义配置
app:
  # 文件上传路径配置
  upload:
    # 本地存储路径
    base-path: /root/studyParty/uploads/
    # 访问URL前缀
    base-url: http://121.199.174.172:8080/static/uploads/
    # 允许的文件类型：图片、音频、视频
    allowed-types: >
      image/jpeg,
      image/png,
      image/gif,
      image/bmp,
      image/webp,
      audio/mpeg,
      audio/wav,
      audio/ogg,
      audio/aac,
      video/mp4,
      video/webm,
      video/ogg,
      video/quicktime
    # 统一默认最大大小（可选）
    max-size: 10

    # 按类型设置最大大小（优先级更高）
    max-sizes:
      image: 10      # 图片最大 10MB
      audio: 50      # 音频最大 50MB
      video: 200     # 视频最大 200MB